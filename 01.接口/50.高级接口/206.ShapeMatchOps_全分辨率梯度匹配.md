---
url: /01.接口/50.高级接口/206.ShapeMatchOps_全分辨率梯度匹配.md
---

# ShapeMatchOps\_全分辨率梯度匹配

## 模块简介

`ShapeMatchOps` 提供 xfxYolo PC 端训练出的 OpenCV 风格 YAML 模板匹配能力，适合在安卓脚本中识别透明形状等目标。

## 功能概览

* 读取一个或多个 PC 端训练出的 `.yaml`
* 支持 `Bitmap` 或图片文件路径作为输入
* 支持全分辨率 `shape_full_points` / `shape_full_features`
* 支持多 YAML、同 label 多模板、统一 NMS、单目标 `max_instances`
* 返回 JSON 字符串，便于懒人精灵和 Lua 解析

## API 接口列表

### 方法清单

| 方法 | 返回 | 说明 |
| --- | --- | --- |
| `matchImageFile(imagePath, yamlPathsJson, threshold, count, pyramidMode, nms, debugCandidates)` | String | 直接读取图片路径和 YAML 路径 JSON 后匹配（Lua 推荐） |
| `matchBitmap(bitmap, yamlPathsJson, threshold, count, pyramidMode, nms, debugCandidates)` | String | 直接读取 Bitmap 和 YAML 路径 JSON 后匹配（Lua 推荐） |
| `matchImageFileFromYamlDir(imagePath, yamlDirPath, threshold, count, pyramidMode, nms, debugCandidates)` | String | 读取目录下全部 YAML 后匹配图片文件 |
| `matchBitmapFromYamlDir(bitmap, yamlDirPath, threshold, count, pyramidMode, nms, debugCandidates)` | String | 读取目录下全部 YAML 后匹配 Bitmap 截图 |
| `loadYaml(names, yamlDirOrFile)` | String | 预加载 YAML，兼容旧用法 |
| `matchImagePath(imagePath, names, sx, sy, ex, ey, threshold, count, pyramidMode, nms)` | String | 使用已预加载 YAML 匹配图片文件 |
| `matchBitmap(bitmap, names, sx, sy, ex, ey, threshold, count, pyramidMode, nms)` | String | 使用已预加载 YAML 匹配 Bitmap |
| `releaseYaml(names)` | Boolean | 释放预加载 YAML，`names` 为空释放全部 |
| `status()` | String | 返回 native 加载状态和已加载 label |
| `nativeVersion()` | String | 返回 native 版本 |
| `detectPolygonsImageFile(imagePath, edgeCount, compareMode, retrievalMode, approxMode, minArea, maxArea, approxEpsilon)` | String | 在图片文件上检测多边形轮廓 |
| `detectPolygonsBitmap(bitmap, edgeCount, compareMode, retrievalMode, approxMode, minArea, maxArea, approxEpsilon)` | String | 在截图 Bitmap 上检测多边形轮廓 |

### matchImageFile

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| imagePath | String | 是 | 待匹配图片路径 |
| yamlPathsJson | String | 是 | YAML 路径 JSON 数组，例如 `["/sdcard/yaml/方向轮盘.yaml"]` |
| threshold | Float | 是 | 分数阈值，范围 `0.0` 到 `1.0` |
| count | Int | 是 | 每个 YAML/模板最多保留的候选数量，多 YAML 调用会按模板数扩展总上限 |
| pyramidMode | String | 是 | 金字塔模式，建议 `"4,8"` |
| nms | Float | 是 | NMS 阈值，建议 `0.3` |
| debugCandidates | Boolean | 是 | 兼容参数，当前不会返回 `debugCandidates` |

### matchBitmap

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| bitmap | Bitmap | 是 | 待匹配 Bitmap，不能为 null 或已回收 |
| yamlPathsJson | String | 是 | YAML 路径 JSON 数组 |
| threshold | Float | 是 | 分数阈值 |
| count | Int | 是 | 每个 YAML/模板最多保留的候选数量，多 YAML 调用会按模板数扩展总上限 |
| pyramidMode | String | 是 | 金字塔模式，建议 `"4,8"` |
| nms | Float | 是 | NMS 阈值 |
| debugCandidates | Boolean | 是 | 兼容参数，当前不会返回 `debugCandidates` |

### matchImageFileFromYamlDir

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| imagePath | String | 是 | 待匹配图片路径 |
| yamlDirPath | String | 是 | YAML 目录路径，例如 `/sdcard/Download/模板小图/yaml/` |
| threshold | Float | 是 | 分数阈值 |
| count | Int | 是 | 每个 YAML/模板最多保留的候选数量，多 YAML 调用会按模板数扩展总上限 |
| pyramidMode | String | 是 | 金字塔模式，建议 `"4,8"` |
| nms | Float | 是 | NMS 阈值 |
| debugCandidates | Boolean | 是 | 兼容参数，目录接口不会返回 `debugCandidates` |

### matchBitmapFromYamlDir

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| bitmap | Bitmap | 是 | 屏幕截图 Bitmap |
| yamlDirPath | String | 是 | YAML 目录路径 |
| threshold | Float | 是 | 分数阈值 |
| count | Int | 是 | 每个 YAML/模板最多保留的候选数量，多 YAML 调用会按模板数扩展总上限 |
| pyramidMode | String | 是 | 金字塔模式，建议 `"4,8"` |
| nms | Float | 是 | NMS 阈值 |
| debugCandidates | Boolean | 是 | 兼容参数，目录接口不会返回 `debugCandidates` |

### detectPolygonsImageFile / detectPolygonsBitmap

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| imagePath | String | 图片路径接口必填 | 待检测图片路径 |
| bitmap | Bitmap | Bitmap 接口必填 | 待检测截图 Bitmap |
| edgeCount | Int | 是 | 目标角数/边数，矩形传 `4` |
| compareMode | Int | 是 | 边数比较方式：`0` 等于、`1` 大于等于、`2` 小于等于 |
| retrievalMode | Int | 是 | 搜索模式，兼容 PC 端参数；当前安卓 Bitmap 实现按外部连接域处理 |
| approxMode | Int | 是 | 轮廓近似方法，兼容 PC 端参数；当前安卓 Bitmap 实现返回连接域矩形四点 |
| minArea | Float | 是 | 最小面积，`-1` 表示不限制；头像框建议 `100` |
| maxArea | Float | 是 | 最大面积，`-1` 表示不限制 |
| approxEpsilon | Float | 是 | 近似逼近阈值，`-1` 表示自动 |

返回 JSON 示例：

```json
{
  "code": 0,
  "message": "ok",
  "imagePath": "/sdcard/Download/测试大图/截图_1785809395573.png",
  "width": 1088,
  "height": 2340,
  "elapsedMs": 42,
  "count": 2,
  "matches": [
    {
      "label": "多边形4",
      "left": 60,
      "top": 1964,
      "right": 150,
      "bottom": 2055,
      "width": 90,
      "height": 91,
      "centerX": 105,
      "centerY": 2009,
      "similarity": 0.93,
      "area": 8190,
      "edgeCount": 4,
      "points": [[60,1964],[150,1964],[150,2055],[60,2055]],
      "index": 0
    }
  ]
}
```

## 返回 JSON 字段

```json
{
  "code": 0,
  "message": "ok",
  "imagePath": "/sdcard/Download/测试大图/adb_20260705_182651_1783247211115_0001.png",
  "width": 2248,
  "height": 1080,
  "elapsedMs": 85,
  "count": 1,
  "matches": [
    {
      "label": "方向轮盘",
      "left": 400,
      "top": 812,
      "right": 500,
      "bottom": 906,
      "width": 100,
      "height": 94,
      "centerX": 450,
      "centerY": 859,
      "score": 0.91
    }
  ]
}
```

## metadata 支持

| 字段 | 说明 |
| --- | --- |
| `search_region` | 搜索中心区域，支持 `coordinate: center_ratio` 和绝对坐标 |
| `max_instances` | 单个 YAML 最多保留实例数 |
| `page_exclusive_group` | 页面互斥分组 |
| `page_exclusive_priority` | 页面互斥优先级，优先级最高的分组会保留 |
| `min_edge_score` | 最低边缘分量 |
| `min_orientation_score` | 最低方向分量 |
| `negative_regions` | 中心点落入后丢弃的负区域 |
| `score_boost` | 最终分数加成 |
| `score_penalty` | 最终分数惩罚 |

默认兼容规则覆盖：金币/钱袋、方向轮盘、盾牌、泉水/双剑、学习技能/加号、普攻/攻击剑、返回、关闭。

## OOP 调用例子

### 从 YAML 路径进行模板匹配

```lua

import('com.nx.assist.lua.LuaEngine')

-- 加载 XfxPlugin.lua 类
local xfxModule = require('lib/XfxPlugin')

-- 创建 XFX 对象实例
local XFX = xfxModule:new({
    apkName = 'xfxPlugin-release.apk',  -- APK 文件名
    context = LuaEngine.getContext(),  -- 上下文对象
})

local opcvOps = XFX:getOps('shapeMatchOps'); assert(opcvOps)

local imagePath = '/sdcard/Download/测试大图/adb_20260705_182651_1783247211115_0001.png'
local yamlDirPath = '/sdcard/Download/模板小图/yaml/'

---- 目录写法：推荐，自动读取目录下全部 yaml
local result = opcvOps.matchImageFileFromYamlDir(imagePath, yamlDirPath, 0.6, 16, '4,8', 0.3, false)

---- 写法一
-- local yamlPathsJson = '["/sdcard/Download/模板小图/yaml/方向轮盘.yaml","/sdcard/Download/模板小图/yaml/盾牌.yaml"]'
-- local result = opcvOps.matchImageFile(imagePath, yamlPathsJson, 0.6, 10, '4,8', 0.3, true)

---- 写法二
local yamlPathsJson = {
	"/sdcard/Download/模板小图/yaml/方向轮盘.yaml",
	"/sdcard/Download/模板小图/yaml/盾牌.yaml",
	"/sdcard/Download/模板小图/yaml/按钮_返回.yaml",
	"/sdcard/Download/模板小图/yaml/按钮_普攻_二值化.yaml",
	"/sdcard/Download/模板小图/yaml/按钮_学习技能.yaml",
	"/sdcard/Download/模板小图/yaml/闭麦.yaml",
	"/sdcard/Download/模板小图/yaml/菜单.yaml",
	"/sdcard/Download/模板小图/yaml/弹窗广告_关闭.yaml",
	"/sdcard/Download/模板小图/yaml/放大镜.yaml",
	"/sdcard/Download/模板小图/yaml/会话气泡.yaml",
	"/sdcard/Download/模板小图/yaml/旗帜.yaml",
	"/sdcard/Download/模板小图/yaml/泉水_双剑.yaml",
	"/sdcard/Download/模板小图/yaml/设置按钮.yaml",
	"/sdcard/Download/模板小图/yaml/战斗画面_金币.yaml",
	"/sdcard/Download/模板小图/yaml/展开.yaml",
}
local result = opcvOps.matchImageFile(imagePath, jsonLib.encode(yamlPathsJson), 0.6, 10, '4,8', 0.3, false)
printEx(result)

local obj = jsonLib.decode(result)
if obj.code == 0 then
  for i, item in ipairs(obj.matches) do
    printEx(item.label .. ' center=(' .. item.centerX .. ',' .. item.centerY .. ') score=' .. item.score)
  end
else
  printEx(obj.message)
end
```

### 从 YAML 目录进行模板匹配

```lua

import('com.nx.assist.lua.LuaEngine')

-- 加载 XfxPlugin.lua 类
local xfxModule = require('lib/XfxPlugin')

-- 创建 XFX 对象实例
local XFX = xfxModule:new({
    apkName = 'xfxPlugin-release.apk',  -- APK 文件名
    context = LuaEngine.getContext(),  -- 上下文对象
})

local opcvOps = XFX:getOps('shapeMatchOps'); assert(opcvOps)

local imagePath = '/sdcard/Download/测试大图/adb_20260705_182651_1783247211115_0001.png'
local yamlDirPath = '/sdcard/Download/模板小图/yaml/'

---- 目录写法：推荐，自动读取目录下全部 yaml
local result = opcvOps.matchImageFileFromYamlDir(imagePath, yamlDirPath, 0.6, 16, '4,8', 0.3, false)
printEx(result)

local obj = jsonLib.decode(result)
if obj.code == 0 then
  for i, item in ipairs(obj.matches) do
    printEx(item.label .. ' center=(' .. item.centerX .. ',' .. item.centerY .. ') score=' .. item.score)
  end
else
  printEx(obj.message)
end
```

### Steam账号切换：检测头像框

```lua
import('com.nx.assist.lua.LuaEngine')

local xfxModule = require('lib/XfxPlugin')
local XFX = xfxModule:new({
    apkName = 'xfxPlugin-release.apk',
    context = LuaEngine.getContext(),
})

local shapeOps = XFX:getOps('shapeMatchOps'); assert(shapeOps)
local imagePath = '/sdcard/Download/测试大图/截图_1785809395573.png'

-- 参数顺序：图片、角数、比较方式、搜索模式、轮廓近似、最小面积、最大面积、近似阈值
local result = shapeOps.detectPolygonsImageFile(imagePath, 4, 0, 0, 2, 100, -1, -1)
printEx(result)

local obj = jsonLib.decode(result)
if obj.code == 0 then
  for i, item in ipairs(obj.matches) do
    -- 头像框一般是左下角两个面积最大的四边形，再结合蓝色“已登录”文字判断另一个未登录账号。
    if item.left < 220 and item.top > 1600 and item.edgeCount == 4 then
      printEx('头像框 center=(' .. item.centerX .. ',' .. item.centerY .. ') area=' .. item.area)
    end
  end
else
  printEx(obj.message)
end
```

## 注意事项

* 推荐优先使用 `matchImageFile` 或新签名 `matchBitmap`，无需手动维护预加载状态。
* 需要全目录识别时，优先使用 `matchImageFileFromYamlDir` 或 `matchBitmapFromYamlDir`，可减少脚本侧枚举和 JNI 往返。
* `yamlPathsJson` 必须显式传入，即使只有一个 YAML 也使用 JSON 数组。
* 当前安卓端复用轻量 native 匹配实现，已同步 metadata、全分辨率 shape 点、edge 粗筛、orientation 复核和 page exclusive 语义；与 PC 端 OpenCV/Numpy 的 `matchTemplate` 细节仍不完全逐像素一致。
* 为获得更稳定性能，建议在 YAML metadata 中显式写入 `search_region`、`max_instances`、`min_edge_score`、`min_orientation_score` 和必要的 `negative_regions`。
